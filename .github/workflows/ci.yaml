name: User CI

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  call-workflow-passing-data:
    uses: chanu-devops/reusable-CICD-yaml/.github/workflows/cicd.yaml@main
    with:
      app-type: nodejs
      micro-services: user